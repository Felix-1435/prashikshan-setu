# PrashikshanSetu — AI accuracy + dual-model fallback pack

Copy these files into your existing repo (paths are relative to project root that contains `server/` and `web/`).

```
server/src/ai.ts          ← replace existing file
docs/AI-FALLBACK-UPDATE.md  ← new (optional documentation)
```

Then push to GitHub. Render will rebuild the API. Vercel web frontend needs no change.

See `docs/AI-FALLBACK-UPDATE.md` for env vars and recommended models.
